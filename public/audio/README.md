# `public/audio`

Static assets loaded by Howler (`/audio/...` from the site root).

- **`shared/`** — cross-feature UI (toasts, etc.)
- **`boss/`** — boss mini-games; see each subfolder README (`handshake/`, `coat-check/`, `paparazzi/`, `white-glove/`).
- **`corkboard/`**, **`intro/`** — shift meta and cinematic flows.
